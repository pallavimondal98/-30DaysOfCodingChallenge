// Explicit type conversion
let strNum = "42";
let intNum = parseInt(strNum); // Converts string to integer

// Implicit type conversion
let num = 10;
let str = "Value is: " + num; // Converts num to a string implicitly
